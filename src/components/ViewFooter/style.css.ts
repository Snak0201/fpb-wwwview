import { style } from "@vanilla-extract/css"

export const styles = {
  footer: style({
    backgroundColor: "aqua",
    textAlign: "center",
  }),
  link: style({
    textDecoration: "none",
  }),
}
