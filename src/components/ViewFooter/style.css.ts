import { style } from "@vanilla-extract/css"

export const styles = {
  footer: style({
    backgroundColor: "aqua",
    textAlign: "center",
    marginTop: "30px",
  }),
  link: style({
    textDecoration: "none",
  }),
}
